import 'package:flutter/material.dart';

class DetailedUserrating extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('DetailedUserrating')
      ),
      body: Center(
        child: Text('DetailedUserrating'),
      ),

    );
  }
}