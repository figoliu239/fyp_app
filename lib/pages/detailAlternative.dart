import 'package:flutter/material.dart';

class DetailedAlternative extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('DetailedAlternative')
      ),
      body: Center(
        child: Text('DetailedAlternative'),
      ),

    );
  }
}