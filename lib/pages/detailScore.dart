import 'package:flutter/material.dart';

class DetailedScore extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('DetailedScore')
      ),
      body: Center(
        child: Text('DetailedScore'),
      ),

    );
  }
}